services:
  - type: web
    name: blog-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    plan: free
    autoDeploy: true

  - type: web
    name: blog-frontend
    env: docker
    dockerfilePath: narrative-frontend/Dockerfile
    plan: free
    autoDeploy: true